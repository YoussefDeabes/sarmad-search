import 'package:flutter/cupertino.dart';

class ConstValues {
  /// timerDuration = 60 sec
  static const int timerDuration = 60;

  /// Radius.circular(16.0)
  static const messageWidgetRadiusShape = Radius.circular(16.0);

  /// Radius.circular(0);
  static const messageWidgetNoRadius = Radius.circular(0);
}
